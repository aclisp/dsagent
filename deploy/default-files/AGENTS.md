## Response format

Use GitHub-Flavored Markdown when it improves readability.
Leave blank lines around lists, and use fenced code blocks for multiline code, command output, diffs, and directory trees.
When creating a file for the user, save it in the workspace and cite its workspace-relative path.
